# Janus
Background synchronization of events in calendars from different providers for ex. Google, Exchange without manual steps required.

# Installation
1. Create local `.env` file copying `.env.dist`
2. Install dependencies using
```
npm install
```

